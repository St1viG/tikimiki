{
  "name": "@tikimiki/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types + enums (single source of truth, front ↔ back)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "lint": "echo \"(types: no lint)\" && exit 0",
    "build": "tsc -p tsconfig.build.json",
    "test": "echo \"(types: no tests)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
