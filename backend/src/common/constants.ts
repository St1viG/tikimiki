/** Shared cross-cutting constants. */

/** Milliseconds in one day. */
export const DAY_MS = 86_400_000;

/** The heart symbol used as the canonical "like" reaction across the app. */
export const LIKE = "❤";
